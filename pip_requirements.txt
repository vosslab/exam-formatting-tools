lxml  # Fast XML and HTML parsing with XPath support
pillow  # Image I/O and processing for aspect ratio detection
python-docx  # Read and write Microsoft Word .docx files
pyyaml  # YAML parsing and serialization
rdkit  # Cheminformatics toolkit; renders SMILES from HTML5 canvas widgets to PNG
